import './App.css';
import Header from './Header';
import Body from './Body';
import Footer from './Footer'

function App() {
  return (
    <div className='App'>
      <Header />
      <Body />
      <Footer />
    </div>
  );
}

export default App;

/**
 * LEARNING PATH
 * 
 * REACT-APP-02-FUNTIONAL-COMPONENTS
 * 
 *  > Added new Header component created with snippet.
 *  > Imported in App.js and used inside HTML return variable.
 *  > Variable has to be in the same capital letter and name from component name.
 * 
 *  > In App.js component function has to has parent html component.
 * 
 */